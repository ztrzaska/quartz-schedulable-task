server.port: 9001
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
  jpa.properties.hibernate.hbm2ddl.auto: create-drop
  quartz:
    job-store-type: jdbc
quartz.enabled: true
